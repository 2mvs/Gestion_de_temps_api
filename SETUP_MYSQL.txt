═══════════════════════════════════════════════════════════════
    CONFIGURATION MYSQL POUR LE BACKEND GTA - GUIDE RAPIDE
═══════════════════════════════════════════════════════════════

📋 ÉTAPE 1 : INSTALLER MYSQL
─────────────────────────────────────────────────────────────

Option A - MySQL Installer (Recommandé) :
  → https://dev.mysql.com/downloads/installer/
  → Installez avec les options par défaut
  → NOTEZ BIEN le mot de passe root que vous définissez !

Option B - XAMPP (Plus Simple pour débutants) :
  → https://www.apachefriends.org/
  → Installez XAMPP
  → Démarrez MySQL dans le panneau de contrôle XAMPP
  → Pas de mot de passe par défaut

Option C - WAMP :
  → https://www.wampserver.com/
  → Installez WAMP
  → Démarrez tous les services
  → Pas de mot de passe par défaut

Vérifier l'installation :
  mysql --version


🗄️ ÉTAPE 2 : CRÉER LA BASE DE DONNÉES
─────────────────────────────────────────────────────────────

Via Ligne de Commande :
  mysql -u root -p
  CREATE DATABASE gta_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
  SHOW DATABASES;
  EXIT;

Via PHPMyAdmin (XAMPP/WAMP) :
  1. Ouvrez http://localhost/phpmyadmin
  2. Cliquez sur "Nouvelle base de données"
  3. Nom : gta_db
  4. Interclassement : utf8mb4_unicode_ci
  5. Cliquez sur "Créer"


⚙️ ÉTAPE 3 : CRÉER LE FICHIER .env
─────────────────────────────────────────────────────────────

1. Allez dans le dossier backend/
2. Copiez le fichier .env.example en .env
   Windows : Copy-Item .env.example .env
   
3. Ouvrez backend/.env et modifiez selon votre configuration :

┌─────────────────────────────────────────────────────────────┐
│  POUR XAMPP/WAMP (sans mot de passe) :                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  DATABASE_URL="mysql://root:@localhost:3306/gta_db"        │
│  JWT_SECRET="secret_jwt_dev_2025_gta"                      │
│  JWT_EXPIRES_IN="7d"                                        │
│  PORT=8008                                                  │
│  NODE_ENV="development"                                     │
│  CORS_ORIGIN="http://localhost:3000"                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  POUR MYSQL STANDARD (avec mot de passe) :                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  DATABASE_URL="mysql://root:votre_password@localhost:3306/gta_db" │
│  JWT_SECRET="secret_jwt_dev_2025_gta"                      │
│  JWT_EXPIRES_IN="7d"                                        │
│  PORT=8008                                                  │
│  NODE_ENV="development"                                     │
│  CORS_ORIGIN="http://localhost:3000"                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘

⚠️  IMPORTANT : Remplacez "votre_password" par votre vrai mot de passe !


🚀 ÉTAPE 4 : INITIALISER LE PROJET
─────────────────────────────────────────────────────────────

Ouvrez un terminal dans le dossier backend/ et exécutez :

  cd backend
  
  npm install
  
  npm run prisma:generate
  
  npm run prisma:migrate
  
  npm run prisma:seed
  
  npm run dev


✅ ÉTAPE 5 : VÉRIFIER QUE TOUT FONCTIONNE
─────────────────────────────────────────────────────────────

Si tout est OK, vous verrez dans le terminal :

  ✅ Connexion à la base de données réussie
  🚀 Serveur démarré sur le port 8008
  📍 Environnement: development
  🌐 API disponible sur: http://localhost:8008/api
  💚 Health check: http://localhost:8008/api/health

Testez dans votre navigateur :
  → http://localhost:8008/api/health

Vous devriez voir : {"status":"OK",...}


🎮 ÉTAPE 6 : DÉMARRER LE FRONTEND
─────────────────────────────────────────────────────────────

Dans un nouveau terminal :

  cd frontend
  npm install
  npm run dev

Frontend disponible sur : http://localhost:3000

Connectez-vous avec :
  Email : admin@gta.com
  Mot de passe : admin123


🐛 PROBLÈMES COURANTS
─────────────────────────────────────────────────────────────

❌ "Access denied for user 'root'@'localhost'"
   → Vérifiez votre mot de passe dans le .env
   → Pour XAMPP/WAMP, essayez sans mot de passe : mysql://root:@localhost:3306/gta_db

❌ "Unknown database 'gta_db'"
   → La base n'a pas été créée
   → Exécutez : mysql -u root -p
                CREATE DATABASE gta_db;
                EXIT;

❌ "Can't connect to MySQL server"
   → MySQL n'est pas démarré
   → XAMPP : Démarrez MySQL dans le panneau de contrôle
   → Services Windows : Cherchez "MySQL" et démarrez le service

❌ "Client does not support authentication protocol"
   → MySQL 8+ utilise un nouveau protocole
   → Solution : mysql -u root -p
                ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'votre_password';
                FLUSH PRIVILEGES;
                EXIT;

❌ "Port 8008 already in use"
   → Changez PORT=8009 dans le .env
   → Ou tuez le processus qui utilise le port 8008


📊 OUTILS UTILES
─────────────────────────────────────────────────────────────

PHPMyAdmin (XAMPP/WAMP) :
  → http://localhost/phpmyadmin
  → Interface web pour gérer MySQL

Prisma Studio :
  → npm run prisma:studio
  → Interface graphique pour voir vos données
  → S'ouvre sur http://localhost:5555

MySQL Workbench :
  → https://dev.mysql.com/downloads/workbench/
  → Interface graphique officielle MySQL


✅ CHECKLIST COMPLÈTE
─────────────────────────────────────────────────────────────

□ MySQL installé et démarré
□ Base de données gta_db créée
□ Fichier backend/.env créé et configuré
□ npm install exécuté
□ npm run prisma:generate exécuté
□ npm run prisma:migrate exécuté
□ npm run prisma:seed exécuté
□ Backend démarre sur http://localhost:8008
□ Health check répond OK
□ Frontend démarre sur http://localhost:3000
□ Login fonctionne avec admin@gta.com


🎉 C'EST PRÊT !
─────────────────────────────────────────────────────────────

Votre application GTA complète est opérationnelle !

Backend : http://localhost:8008/api
Frontend : http://localhost:3000

Compte de test :
  Email : admin@gta.com
  Password : admin123


📚 DOCUMENTATION COMPLÈTE
─────────────────────────────────────────────────────────────

→ backend/SETUP_MYSQL.md        : Guide détaillé MySQL
→ backend/README.md              : Documentation complète
→ backend/QUICKSTART.md          : Guide de démarrage
→ backend/FRONTEND_INTEGRATION.md : Guide d'intégration
→ backend/INDEX.md               : Index de toute la doc

═══════════════════════════════════════════════════════════════
              Bon développement ! 🚀
═══════════════════════════════════════════════════════════════

